programa {

/*Criar um programa que simule o login de um roteador. O nome de usuário (username) é "admin" e a senha (password) "123". 
Pedir ao usuário para digitar username e password. Caso os dados estejam corretos, mostrar uma mensagem "Login efetuado!",
 caso contrário "Login falhou!". (DESAFIO: Mostrar mensagens específicas para erro de username, de password ou de ambos).  */
  funcao inicio() {
   cadeia senha, username

   escreva("Digite o nome de usuário: ")
   leia(username)
   escreva("Digite a senha de de login: ")
   leia(senha)

   se(username != "admin"){
    escreva("Nome de usuário incorreto!\n")
   }
   se ( senha != "123"){
    escreva("Senha incorrta!\n")
   }
   se(username != "admin" e  senha != "123"){
    escreva("Login falhou!\n")
   }
   se(username == "admin" e  senha == "123"){
    escreva("Login efetuado!")
   }
  }
}
